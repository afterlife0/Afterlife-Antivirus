<Page x:ConnectionId='1'
    x:Class="AfterlifeWinUI.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="Transparent"
                        
                            >
    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="0,0,8,0">
        <StackPanel Spacing="20">
            <!-- Header - Animated Element 0 -->
            <StackPanel x:ConnectionId='2' x:Name="AnimHeader" Margin="0,16,0,0">
                <StackPanel Orientation="Horizontal" Spacing="10">
                    <FontIcon Glyph="&#xE713;" FontSize="28" Foreground="{StaticResource CyanBrush}"/>
                    <TextBlock Style="{StaticResource TitleText}" Text="Settings"/>
                </StackPanel>
                <TextBlock x:ConnectionId='26' x:Name="PageSubtitle" Style="{StaticResource SubtitleText}" Text="Configure protection settings and preferences" Margin="0,4,0,0"/>
            </StackPanel>

            <!-- Appearance / Theme Section -->
            <Border x:ConnectionId='3' x:Name="AnimSection0" Style="{StaticResource GlassPanel}">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#1815151f" Offset="0"/>
                        <GradientStop Color="#08ffffff" Offset="0.3"/>
                        <GradientStop Color="#1812121a" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <StackPanel Spacing="16">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE790;" FontSize="18" Foreground="{StaticResource OrangeBrush}"/>
                        <TextBlock x:ConnectionId='25' x:Name="AppearanceTitle" Text="Appearance" FontWeight="SemiBold" FontSize="16" Foreground="White"/>
                    </StackPanel>
                    
                    <TextBlock x:ConnectionId='18' x:Name="ThemeLabel" Text="Theme" Foreground="{StaticResource TextSecondaryBrush}" FontSize="13"/>
                    
                    <!-- Theme Selector - Three Radio Buttons -->
                    <StackPanel Orientation="Horizontal" Spacing="12">
                        <!-- System Theme -->
                        <Border x:ConnectionId='19' x:Name="ThemeSystem" 
                                Background="#15ffffff" CornerRadius="12" Padding="16,12"
                                BorderThickness="2" BorderBrush="Transparent"
                                                                  >
                            <StackPanel Orientation="Horizontal" Spacing="10">
                                <FontIcon Glyph="&#xE770;" FontSize="20" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBlock x:ConnectionId='24' x:Name="SystemThemeText" Text="System" Foreground="{StaticResource TextPrimaryBrush}" 
                                           VerticalAlignment="Center" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Border>
                        
                        <!-- Dark Theme -->
                        <Border x:ConnectionId='20' x:Name="ThemeDark" 
                                Background="#15ffffff" CornerRadius="12" Padding="16,12"
                                BorderThickness="2" BorderBrush="{StaticResource CyanBrush}"
                                                                >
                            <StackPanel Orientation="Horizontal" Spacing="10">
                                <FontIcon Glyph="&#xE708;" FontSize="20" Foreground="{StaticResource PurpleBrush}"/>
                                <TextBlock x:ConnectionId='23' x:Name="DarkThemeText" Text="Dark" Foreground="{StaticResource TextPrimaryBrush}" 
                                           VerticalAlignment="Center" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Border>
                        
                        <!-- Light Theme -->
                        <Border x:ConnectionId='21' x:Name="ThemeLight" 
                                Background="#15ffffff" CornerRadius="12" Padding="16,12"
                                BorderThickness="2" BorderBrush="Transparent"
                                                                 >
                            <StackPanel Orientation="Horizontal" Spacing="10">
                                <FontIcon Glyph="&#xE706;" FontSize="20" Foreground="{StaticResource OrangeBrush}"/>
                                <TextBlock x:ConnectionId='22' x:Name="LightThemeText" Text="Light" Foreground="{StaticResource TextPrimaryBrush}" 
                                           VerticalAlignment="Center" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                    
                    <TextBlock Text="Theme changes apply immediately" 
                               Foreground="{StaticResource GreenBrush}" FontSize="11" FontStyle="Italic"/>
                </StackPanel>
            </Border>

            <!-- Real-time Protection -->
            <Border x:ConnectionId='4' x:Name="AnimSection1" Style="{StaticResource GlassPanel}">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#1815151f" Offset="0"/>
                        <GradientStop Color="#08ffffff" Offset="0.3"/>
                        <GradientStop Color="#1812121a" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <StackPanel Spacing="12">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE8C9;" FontSize="18" Foreground="{StaticResource CyanBrush}"/>
                        <TextBlock x:ConnectionId='17' x:Name="RealTimeTitle" Text="Real-time Protection" FontWeight="SemiBold" FontSize="16" Foreground="White"/>
                    </StackPanel>
                    <ToggleSwitch x:ConnectionId='14' x:Name="ToggleRealTime" Header="Enable real-time scanning"                                 />
                    <ToggleSwitch x:ConnectionId='15' x:Name="ToggleScanOnCreation" Header="Scan on file creation"                                       />
                    <ToggleSwitch x:ConnectionId='16' x:Name="ToggleScanOnModification" Header="Scan on file modification"                                           />
                </StackPanel>
            </Border>

            <!-- Detection Engines -->
            <Border x:ConnectionId='5' x:Name="AnimSection2" Style="{StaticResource GlassPanel}">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#1815151f" Offset="0"/>
                        <GradientStop Color="#08ffffff" Offset="0.3"/>
                        <GradientStop Color="#1812121a" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <StackPanel Spacing="12">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE773;" FontSize="18" Foreground="{StaticResource GreenBrush}"/>
                        <TextBlock x:ConnectionId='13' x:Name="DetectionTitle" Text="Detection Engines" FontWeight="SemiBold" FontSize="16" Foreground="White"/>
                    </StackPanel>
                    <ToggleSwitch x:ConnectionId='10' x:Name="ToggleSignature" Header="Signature-based detection"                                  />
                    <ToggleSwitch x:ConnectionId='11' x:Name="ToggleYara" Header="YARA rule matching"                             />
                    <ToggleSwitch x:ConnectionId='12' x:Name="ToggleAI" Header="AI heuristic analysis"                           />
                </StackPanel>
            </Border>

            <!-- Scheduled Scans -->
            <Border x:ConnectionId='6' x:Name="AnimSection3" Style="{StaticResource GlassPanel}">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#1815151f" Offset="0"/>
                        <GradientStop Color="#08ffffff" Offset="0.3"/>
                        <GradientStop Color="#1812121a" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <StackPanel Spacing="12">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE787;" FontSize="18" Foreground="{StaticResource PurpleBrush}"/>
                        <TextBlock x:ConnectionId='9' x:Name="ScheduledTitle" Text="Scheduled Scans" FontWeight="SemiBold" FontSize="16" Foreground="White"/>
                    </StackPanel>
                    <ToggleSwitch x:ConnectionId='7' x:Name="ToggleScheduled" Header="Enable scheduled scans"                                  />
                    <StackPanel Orientation="Horizontal" Spacing="12">
                        <TextBlock x:ConnectionId='8' x:Name="ScanIntervalLabel" Text="Scan interval:" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                        <ComboBox Width="120" SelectedIndex="0">
                            <ComboBoxItem Content="1 hour"/>
                            <ComboBoxItem Content="6 hours"/>
                            <ComboBoxItem Content="12 hours"/>
                            <ComboBoxItem Content="24 hours"/>
                        </ComboBox>
                    </StackPanel>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>

